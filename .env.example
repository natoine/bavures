# URI de connexion MongoDB (obligatoire)
MONGODB_URI=mongodb://localhost:27017/bavures

# Nom de la base de données (optionnel, défaut : "bavures")
MONGODB_DB=bavures

# Dossier de stockage des documents sources téléchargeables (optionnel,
# défaut : <projet>/data/sources)
DATA_SOURCES_DIR=
